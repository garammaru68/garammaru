#include "JosephsProblemHead.h"

NODE* Circle(char* manList)
{
	int allMan;

	NODE *pNode = 0;

	for (NODE* i = 0; i <  allMan; i++)
	{
		pNode 
		pNode->next = (NODE*)malloc(sizeof(NODE));
		pNode = pNode->next;
		pNode->man = manList[i];
		
	}
}

void main()
{
	int *count;

	printf("탈출할 병사 : ");
	scanf("%d\n", &allMan);

	printf("간격 : ");
	scanf("%d\n", &count);
}

